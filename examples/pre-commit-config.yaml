# Example pre-commit configuration demonstrating all py-review-rules rules.
#
# For a released package, replace `repo: local` with this repository URL and pin
# `rev` to the version you want to use.
repos:
  - repo: local
    hooks:
      - id: py-review-rules
        name: py-review-rules
        entry: uv run py-review-rules check
        language: system
        types: [python]
        args:
          - --max-sloc=1000
          - --max-func-sloc=500
          - --max-class-sloc=1000
          - --max-method-sloc=300
          - --max-function-nesting-depth=0
          - --max-constructs=1
          - --max-classes=1
          - --max-funcs=1
          - --max-local-helpers=2
          - --banned-import=osgeo, pyyaml->ruamel.yaml, pyaml->ruamel.yaml
          - --max-args=9
          - --max-methods=10
          - --max-class-members=10
          - --require-class-member-hint
          - --require-class-doc
          - --require-function-doc
          - --require-top-level-import
          - --ban-future-import
          - --require-type-hint
          - --ban-typing-alias
          - --ban-mutable-global
          - --ban-pass-only-except
          - --ban-output-construct
          - --ban-placeholder-comment
          - --ban-placeholder-pass
          - --ban-notimplemented-placeholder
          - --max-bool-args=3
          - --require-kw-only-defaults
          - --max-elifs=4
          - --max-nested-ifs=2
